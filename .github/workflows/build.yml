name: 'Build'
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '["qwerty"]'
      nav: '["vi"]'
      clipboard: '["win"]'
      layers: '["default"]'
      mapping: '["default"]'
      kmonad_os: '["win"]'
